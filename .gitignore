# Secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/

# Claude Code local state (project-level)
.claude/settings.local.json

# Assistant session memory accidentally written into the repo instead of
# the user-level Claude Code memory store — local/session state, not a
# project artifact, and may contain notes about working style.
memory/

# Intelligence-systemet
data/*.db
data/*.db-journal

# OS
Thumbs.db
.DS_Store
